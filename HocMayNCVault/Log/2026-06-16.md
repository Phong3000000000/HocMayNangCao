---
type: daily-log
tags: [log/daily]
created: 2026-06-16
---

# 📋 Log ngày 2026-06-16

## Task đã hoàn thành

### 1. Phân tích hiệu năng huấn luyện 100K episodes và tư vấn chiến lược 200K episodes
**Mô tả**: Phân tích câu hỏi của người dùng về việc có nên tăng số lượng tập huấn luyện từ 100,000 lên 200,000 episodes hay không khi RL Agent vẫn chưa vượt qua Heuristic Agent (Reorder Threshold: luôn đặt hàng là 5 khi inventory < 5).

- Đưa ra kết luận **Không nên** tăng lên 200,000 episodes một cách đơn thuần do bão hòa thuật toán và phân bố Epsilon decay không còn tối ưu.
- Chỉ rõ lý do Heuristic hoạt động quá xuất sắc (tỷ lệ phạt hết hàng gấp 16 lần chi phí lưu kho làm chiến lược phòng thủ an toàn cao là tối ưu tuyệt đối).
- Phát hiện không gian trạng thái bị loãng gấp 7 lần do biến `day_of_week` không cần thiết trong quá trình huấn luyện tiêu chuẩn.
- Đề xuất các giải pháp cải thiện cụ thể: thu hẹp không gian trạng thái (bỏ `day_of_week` khi train), áp dụng reward shaping, và tinh chỉnh epsilon decay thích ứng.

**File tạo/sửa**:
- [[Notes/PhanTich_HieuNang_RL_100K_200K]] [NEW] — File phân tích chi tiết hiệu năng và hướng cải thiện.
- [[Notes/Index]] [MODIFY] — Cập nhật liên kết đến file phân tích mới.
- [custom_env.py](file:///d:/HocTap/HocMayNangCao/envs/custom_env.py) [MODIFY] — Thêm tham số `surge_days` để tùy biến các ngày đột biến nhu cầu trong tuần (mặc định tương thích ngược với `weekend_surge`).
- [test_env.py](file:///d:/HocTap/HocMayNangCao/tests/test_env.py) [MODIFY] — Bổ sung lớp `TestCustomSurgeDays` (4 test cases) và chạy thành công 54/54 tests.
- [app.py](file:///d:/HocTap/HocMayNangCao/dashboard/app.py) [MODIFY] — Sắp xếp lại thứ tự widget trên Sidebar và chuyển ô chọn "Demand Regime" thành trường thông tin hiển thị chỉ xem (Read-Only); tích hợp widget "Select Surge Days"; điều chỉnh tỷ lệ trục X của biểu đồ học tập; và **tối ưu hóa chia nhỏ (chi tiết hóa) các vạch chia (ticks) trên trục X (tất cả 30 ngày) và trục Y** cho 5 biểu đồ mô phỏng đơn episode để người dùng dễ theo dõi trực quan hơn.
- [shrink_histories.py](file:///d:/HocTap/HocMayNangCao/tmp/shrink_histories.py) [NEW] — Tạo script tối ưu dung lượng (chỉ giữ lại `episode_rewards`, `epsilons` và thực hiện downsample hệ số 50) cho các file training history trong cả `results` (20K) và `results_100k` (100K) để giảm dung lượng file xuống dưới 1MB, giúp push GitHub thành công và hiển thị mượt mà trên dashboard.
- **Git Push**: Hoàn thành `git commit --amend` tích hợp code mới và các file lịch sử đã thu gọn, sau đó push thành công lên GitHub repository.

## Liên kết
- [[Notes/PhanTich_HieuNang_RL_100K_200K]] — File phân tích chi tiết.
- [[Notes/DanhGia_DuAn]] — Đánh giá dự án.
